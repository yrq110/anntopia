---
name: Convex Hull
menu: Geometry
route: /geometry/convex-hull
---

import { Playground, Props } from 'docz'
import { GrahamScan, JarvisMarch, Chan } from '@anntopia/geometry'
import Canvas from '../components/Canvas'
import { uniform } from '../utils/randomPoints'

## Overview

Todo

* All returned polygon's vertices are in anti-clockwise

## GrahamScan

<Playground>
  {
    () => {
      // import { GrahamScan } from '@anntopia/geometry'
      const points = uniform(40)
      const poly = GrahamScan(points)
      return  <Canvas points={points} polygons={[poly]}/>
    }
  }
</Playground>

## JarvisMarch

<Playground>
  {
    () => {
      // import { JarvisMarch } from '@anntopia/geometry'
      const points = uniform(40)
      const poly = JarvisMarch(points)
      return  <Canvas points={points} polygons={[poly]}/>
    }
  }
</Playground>


## Chan

<Playground>
  {
    () => {
      // import { Chan } from '@anntopia/geometry'
      const points = uniform(40)
      const poly = Chan(points)
      return  <Canvas points={points} polygons={[poly]}/>
    }
  }
</Playground>
